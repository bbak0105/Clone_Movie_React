import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);

// App 컴포넌트를 ReactDOM에 id가 root인 엘리먼트에 그린다고 생각하면 된다.
// 리액트는 우리가 작성한 프로젝트 폴더에 있는 코드를 자바스크립트를 이용하여 해석한다.
// 그리고 해석한 결과물을 index.html로 끼워 넣는다.

